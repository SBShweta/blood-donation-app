apiVersion: v1
kind: Service
metadata:
  name: blood-donation-backend
  labels:
    app: blood-donation-backend
spec:
  selector:
    app: blood-donation-backend
  ports:
  - port: 5000
    targetPort: 5000
  type: ClusterIP